<Page x:Class="View.GameOverPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="GameOverScreen" Height="450" Width="800">
    <Page.Resources>

    </Page.Resources>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="100"></RowDefinition>
                <RowDefinition Height="100"></RowDefinition>
                <RowDefinition Height="100"></RowDefinition>
                <RowDefinition Height="100"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                
            </Grid.ColumnDefinitions>


        <Label Name="ResultLabe1l" Content="{Binding Path=Result}" Grid.Row="1" HorizontalAlignment="Center" FontSize="40"/>
        <Label Name="Winner" Content="{Binding Path=WinnerScore}" Grid.Row="2" HorizontalAlignment="Center" FontSize="20"/>
        <Label Name="Loose" Content="{Binding Path=LooserScore}" Grid.Row="3" HorizontalAlignment="Center" FontSize="20"/>
        <Button Name="asd" Content="Back to the Lobby" Command="{Binding Path=BackToLobbyCommand}" Grid.Row="4" HorizontalAlignment="Center" Width="100" Height="20"/>
    </Grid>
</Page>
